platform :ios, '14.0'
use_frameworks! :linkage => :static

target 'U1Slicer' do
  # Note: Kotlin Multiplatform framework is built separately
  # Run ./ios/build-shared-framework.sh first, then add the framework
  # manually in Xcode: U1Slicer target → General → Frameworks

  # If you need additional iOS dependencies, add them here
  # pod 'Alamofire', '~> 5.8'
end
